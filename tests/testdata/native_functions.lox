print clock;

var t1 = clock();
var t2 = clock();

print t2 > t1;        // Asserting positively increasing time
print t2 - t1 < 1;    // Our language can't be /that/ slow

print dir;

class A {
  foo() {}
}
class B < A{
  bar() {}
  foo() {}
}
class C < B {
  baz() {}
  bruh() {}
}
print dir(B);
print dir(C());
